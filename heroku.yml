# build:
#   docker:
#     web: Dockerfile
# run:
#   web: bundle exec puma -C config/puma.rb

build:
  docker:
    web: Dockerfile
  config:
    RAILS_ENV: development
run:
  web: bundle exec puma -C config/puma.rb
