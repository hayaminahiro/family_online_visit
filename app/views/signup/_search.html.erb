<%= form_with url: step3_signup_index_path, method: :get, local: true do |f| %>
	<%= f.text_field :postal_code, class: 'fa input is-medium input-top' %>
	<%= f.submit "住所を入手する", class: "button is-link is-medium is-fullwidth new-btn has-text-weight-bold" %>
<% end %>